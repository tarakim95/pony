# CLAUDE.md

> 이 파일은 매 작업 시작 시 가장 먼저 읽는다. 그 다음 `progress.md`를 읽어 현재 상태를 파악한다.

## 프로젝트 정체성

**이름**: Pony (흐름)
**한 줄 설명**: 트렌드 / 문화 / 사회 현상에서 일어나는 표면 사건들을 자동 수집해 *밑에 흐르는 공통 흐름*으로 연결해 매일 브리핑하는 도구. 정치/경제 헤드라인 자체가 목적은 아님 — 사람들이 뭐에 꽂혀있는지, 무드가 어디로 가는지를 본다.
---

## 에이전트 역할 분리

- **Claude.ai 채팅 (Opus 4.7)** = 설계/기획 역할. 큰 결정, 데이터 모델 변경, 새 기능 기획, 어려운 디버깅, 주차 마감 검토.
- **Claude Code (Sonnet 4.6/4.7)** = 실행 역할. 일반 코딩, 테스트, 디버깅, 문서 업데이트.
- **Subagent**: 반복 작업 전용 (예: 테스트 작성). 작은 작업엔 부르지 말 것 (컨텍스트 전달 비용이 더 큼).
- **호출 기준**: "큰 결정인지 작은 결정인지" 헷갈리면 → Claude Code가 먼저 시도. 막히거나 설계 갈림길이면 그때 채팅으로 가져오기.

---

## 개발 원칙 (HARD RULES)

### 1. 파일 분할
- 모든 코드 파일은 **500줄 이하**. 넘으면 즉시 분리.
- 파일이 *하나의 명확한 책임*만 가질 것. 책임이 두 개면 분리 신호.
- 분리 시 폴더 구조 따라가기 (예: `lib/engines/daily.ts` 길어지면 `lib/engines/daily/` 폴더로).
- 분리 시 순환 참조(circular import) 금지. 의존 방향은 항상 단방향.

### 2. TDD 의무
- **모든 새 기능은 테스트부터**. 구현 코드 먼저 쓰지 말 것.
- 순서: 실패하는 테스트 작성 → 최소 구현 → 통과 확인 → 리팩터.
- 예외: LLM 응답 자체는 결정론적이지 않음 → *프롬프트 입출력 형태* 검증 + *후처리 로직*만 테스트. LLM 자체는 별도 평가 스크립트로.

### 3. 메모리 / 실수 기록
- 같은 실수 반복하지 않도록 `docs/learnings.md`에 기록.
- 형식: 날짜 · 무엇이 잘못됐나 · 왜 그랬나 · 다음에 어떻게 막을지.
- 매 작업 시작 시 `learnings.md`도 빠르게 훑기.

### 4. progress.md 갱신
- 매 작업 끝에 `progress.md` 갱신.
- 다음 세션이 *progress.md만 읽고도 이어갈 수 있도록* 작성.
- 형식: 마지막 작업 / 현재 상태 / 다음 할 일 / 막힌 곳 / 결정 미룬 항목.

### 5. 문서화 동시 갱신
- 데이터 스키마 변경 → `docs/SPEC.md` 동시 갱신.
- 새 기능 추가 → `docs/SPEC.md` 동시 갱신.
- 코드와 문서가 어긋나면 *문서가 옳고 코드를 고침*. (문서가 의도)

### 6. 시크릿 절대 금지
- API 키, DB 패스워드, 토큰 — 코드에 직접 적지 않음.
- 모든 시크릿은 `.env.local` (로컬) / Vercel 환경변수 (배포).
- `.env.example`만 깃에 올림. 진짜 값은 절대 안 됨.
- 매 커밋 전 `git diff`로 시크릿 노출 안 됐는지 확인.

### 7. 비용 안전장치
- LLM 호출은 모두 `lib/llm/client.ts`를 거침. 다른 경로 금지.
- 클라이언트가 *일일 호출 카운트* 추적. 한도 초과 시 자동 중단.
- 한도: 일일 100회 / 월 2,000회 (조정 가능). 한도 도달 시 알림.
- 무한 루프 방어: 같은 함수 내 LLM 호출 ≤ 5회.

### 8. 데이터 보호 / 마이그레이션
- 메모(notes) 테이블은 *별도 export 가능* 구조 유지 (사용자 자산).
- 매주 일요일 자동 백업 (Supabase 백업 기능 활용).

**마이그레이션 절차** (현재 무료 단일 DB 환경 기준):
1. 마이그레이션 파일 작성 + git 커밋
2. Supabase SQL Editor에서 `BEGIN; ... ROLLBACK;` 시뮬레이션 실행 → 컬럼/인덱스 결과 확인
3. 검증 통과하면 `BEGIN; ... COMMIT;` 진짜 적용
4. 적용 직후 *동작 테스트* 필수 (unique 제약이면 중복 INSERT, FK면 위반 INSERT 등)
5. 롤백 SQL 미리 메모해두기 (재앙 대비)

**dev/prod 분리되면** (Supabase 유료 전환 또는 별도 dev 프로젝트 생성 시):
- dev에서 1~4 먼저 → 통과 시 prod에 동일 절차 반복

**금지**:
- 마이그레이션 파일 작성만 하고 "동작 검증 = 파일 존재" 로 착각하지 말 것
- prod에 직접 BEGIN 없이 ALTER 던지지 말 것 (롤백 불가)

### 9. Git 전략
- `main` = 항상 동작 가능한 상태.
- `feat/*`, `fix/*`, `refactor/*` 브랜치에서 작업 → main에 squash merge.
- 커밋 메시지: `[영역] 무엇을` (예: `[engine] 일일 흐름 감지 프롬프트 1차`).
- 매 phase 끝에 git tag (`phase-1-done` 등).

### 10. 앱 전환 기준
- React Native 전환은 다음 모두 만족 시에만:
  - PWA 사용 *최소 8주 이상* 누적
  - 매일 1회 이상 사용 *4주 이상* 유지
  - 메모 50개 이상 누적
  - "RN 전환 없이 못 쓰겠다"는 명확한 페인 포인트 발생
- 그 전에는 RN 코드 짜지 않음. 데이터 구조만 호환.

### 11. 컨텍스트 효율화
- 매 세션 시작 시 읽기 순서: `CLAUDE.md` → `progress.md` → `docs/learnings.md` → 작업 관련 코드.
- 긴 파일 전체 보지 말고 관련 함수만 보기.
- 불필요한 검색 줄이기 — 폴더 구조에 따라 위치 추측 후 직접 접근.

### 12. 작업 흐름
- 설명을 최소화하고 바로 실행한다. 진행 보고는 간결하게.
- 작업 단위 완료 시 git add + commit. push는 사용자 명시적 OK 후에만.
- 항상 feat/* 또는 fix/* 브랜치에서 작업. main에 직접 push 금지.
- .env, .env.local 등 시크릿 파일은 어떤 경우에도 커밋 금지. 매 commit 전 git diff로 확인.

---

## 기술 스택 (확정)

| 레이어 | 선택 | 이유 |
|---|---|---|
| Frontend | Next.js 15 + TypeScript | RN 전환 시 React 컴포넌트 그대로 활용 |
| 스타일 | Tailwind CSS | shadcn/ui 호환, RN에선 NativeWind로 옮김 |
| 배포 | Vercel | 무료, 서버리스 함수 통합 |
| DB | Supabase (Postgres) | 무료, RLS 지원, pgvector 내장, RN 클라이언트 있음 |
| 인증 | Supabase Auth | 멀티유저 대비 |
| 벡터 검색 | pgvector | 흐름 그룹 매칭, RAG |
| LLM | Anthropic Claude API | Haiku 기본, Sonnet은 메타 분석/심층 |
| 스케줄러 | Vercel Cron | 매일 자동 실행 |
| 폰트 | Pretendard (CDN) | 디자인 합의 |
| PWA | next-pwa | 홈 화면 추가, iOS 16.4+ 푸시 |

---

## 색상 팔레트 (확정)

```
--espresso:      #3E2723   /* 메인 텍스트 + 강조 */
--espresso-soft: #5C3F38   /* 보조 텍스트 */
--peony:         #F4C9D6   /* 액센트 + 강도 표시 */
--peony-deep:    #E8A8BA   /* 강한 액센트 */
--linen:         #F5F1E6   /* 배경 */
--linen-warm:    #EDE6D4   /* 카드 배경 */
--cream:         #FAF7EE   /* 상승된 카드 */
```

폰트: **Pretendard만**. 위계는 weight (400/500/600/700/800)로만. 세리프 금지.

---

## 핵심 데이터 모델 (요약)

상세는 `docs/SPEC.md` 참조. 빠르게 보면:

- `streams` — 매일 생성되는 흐름 카드. 카드 한 장 = 한 row.
- `events` — 흐름을 구성하는 표면 사건들 (다이어그램의 점).
- `stream_events` — 흐름과 사건의 다대다 관계.
- `stream_groups` — 같은 흐름의 시간 묶음 (추적 모드 핵심).
- `saved_streams` — 사용자가 저장한 흐름.
- `notes` — 사용자가 흐름에 단 메모. **이게 사용자 자산**.
- `user_profile` — 맞춤 함의 생성용.

---

## 호출 정책 (Phase 1까지 잠정)

- **자동 실행 엔진 (일일/메타)**: Phase 1에서 Haiku와 Sonnet 둘 다 시도하고 품질을 비교한 후 결정. Phase 1 끝 (Week 1 마지막)에 평가하여 모델 확정. 결정 후엔 임의 변경 금지.
- **임베딩**: 가장 저렴한 옵션 사용. Anthropic은 임베딩 API 별도 없음 → Voyage AI 또는 OpenAI text-embedding-3-small 중 Phase 1에서 결정.
- **인간 채팅 (head agent)**: Opus 4.7. Claude.ai에서만 사용. 코드 안에서 호출 금지.
- **Claude Code 코딩**: Sonnet 4.6 또는 4.7. Haiku로 임의 변경 금지.

---

## "이건 절대 하지 말 것"

- localStorage / sessionStorage 사용 (PWA 환경 일부에서 깨짐)
- LLM 응답을 그대로 DB에 박기 (반드시 스키마 검증 후 저장)
- prod DB로 실험
- 시크릿을 코드에 인라인
- "임시로" 하드코딩 (나중에 안 고침)
- 500줄 넘는 파일 방치
- 테스트 없이 새 기능 머지
- progress.md 빼먹기

---

## "막힐 때 어떻게 하는가"

1. 같은 문제 `learnings.md`에 있는지 검색
2. 없으면 작은 재현 케이스 만들기
3. 30분 시도해도 안 풀리면 → Claude.ai 채팅으로 가져와서 의논
4. 풀린 후 → `learnings.md`에 기록
